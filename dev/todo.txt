- ufft.py, test_ufft.py : Array API compliance
  refactor done, but there are problems

  u2u():
  + U2USpecialCase works, except
    often numerical issues w/ torch
    sometimes w/ jax
  + U2UMixCase
    numerical issues on all backends

  -> CZT() imprecision is the cause.
  What operations in CZT() are problematic?
  Hypotheses:
  - xp.flip() indices wrong? -> no, correct
    so it is a precision problem somewhere below [or beyond]
  - xp.fft.fft[n]() ?
  - xp.exp() or **

  focus on special case first: what is causing jax/torch to break? Modulation?
  modulation can also be at cause in czt()

  if not check FFT accuracy.




  also need to test gpu backends

  tag v0.2


- jax/torch JIT compilation [ufft only]
  Specific to jax/torch backends: assess JIT-ability of array-accepting functions.
  array_api_extra has helpers for this?

  also ensure gradients wrt `w` works.

  tag v0.3


- nufft.py, test_nufft.py
  * make optional dependency.
  * Array API compliance
  * Add JAX support via buffer_callback() [or use jax-finufft?]
    + background on buffer_callback(): https://github.com/ROCm/jax/commit/7c2696545d48947e8f5e157a3857687eb2646106
    + buffer_callback() source code: https://github.com/jax-ml/jax/blob/210b5fc8674e4993254c804720144c570992984e/jax/_src/buffer_callback.py#L38-L129
  * Add PyTorch support via [built-in feature] or [external package?]

  derivatives wrt (x/v, w) should work

  tag v0.4 -> v1.0 after extensive use in optikal


- Add MATLAB interface
  Must work with CPU/GPU arrays.
  Distribute this as a separate package, or piggy-back somehow on Python bindings [/w copy on GPU backend.]
