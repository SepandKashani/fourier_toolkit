- util.py :: move fully to array api standard
  uniformspec left, rest ok
  test suite except uniformspec also updated
  start w/ conftest.py since useful for uniformspec
  function to transform inputs to specific backend?

  .. code
     import array_api_compat as aac
     xp = aac.array_namespace(x)
     info = xp.__array_namespace_info__()

     info.dtypes()[dtype:str] -> dtype object
     or
     xp.[float32|64,complex64|128,int32|64]

     if hasattr(xp, 'fft')
       can use module to perform FFTs

     if hasattr(xp, 'linalg')
       can use module for standard linalg operators.


- ufft:: move to array_api standard
  fft codes: accept camp in/out only to abide by array api


- Add CuPy Support [superseded by array_api usage]

  2. test_ufft.py
     Modify TestCZT().test_[value_apply, prec]() to
     - respect backend;
     - respect GPU choice;

     Ensure ct.xxx() calls have all inputs on same device.

  3. test_ufft.py
     Modify TestU2U().test_[value_apply, prec]() to
     - respect backend;
     - respect GPU choice;

    Ensure ct.xxx() calls have all inputs on same device.

  4. Tag as v0.2

  5. test_nufft.py
     Adding GPU support needs more thought/work


- Add JAX Support via buffer_callback()
  + background on buffer_callback(): https://github.com/ROCm/jax/commit/7c2696545d48947e8f5e157a3857687eb2646106
  + buffer_callback() source code: https://github.com/jax-ml/jax/blob/210b5fc8674e4993254c804720144c570992984e/jax/_src/buffer_callback.py#L38-L129

  Tag as v0.3

- Add MATLAB interface
  Must work with CPU/GPU arrays.
  Distribute this as a separate package, or piggy-back somehow on Python bindings [/w copy on GPU backend.]
